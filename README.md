# password-generator-python
A simple Python command-line weather application that shows current weather using the OpenWeather API. Files
# Password Generator (Python)

A simple Python project that generates strong random passwords using letters, numbers, and symbols.

## Features
- Generate random passwords
- Custom password length
- Uses letters, numbers, and symbols

## Technologies Used
- Python

## How to Run
1. Install Python.
2. Run:
   python password_generator.py
